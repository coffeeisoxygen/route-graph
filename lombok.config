# Root Configuration
config.stopBubbling = true

# Code Generation
lombok.addLombokGeneratedAnnotation = true
lombok.anyConstructor.addConstructorProperties = true

# Accessor Settings
lombok.accessors.chain = true
lombok.accessors.fluent = false

lombok.getter.noIsPrefix = false  # Changed to false to ensure proper getter generation

# Class Behavior
lombok.equalsAndHashCode.callSuper = CALL
lombok.toString.callSuper = CALL
lombok.builder.className = Builder

# Annotation Usage Control
lombok.experimental.flagUsage = ALLOW

# Null Safety
lombok.nonNull.exceptionType = IllegalArgumentException

# Logging Configuration
lombok.log.fieldIsStatic = true
lombok.log.fieldName = log
lombok.log.slf4j.flagUsage = ALLOW
lombok.log.custom.flagUsage = ALLOW
